---
name: project-naming-pcfix
description: Permanently mandates project name as pcfix (PCFix), strictly forbidding pc fixit or pc-fixit
metadata:
  type: feedback
---

# Mandated Project Name: pcfix

The project, branding, desktop app, mockups, and internal labels must strictly be called **pcfix** (or PCFix in title case).

**Why:** The user explicitly commanded: "ive told you to change all the thing that called pc fixit to pcfix. change it now". Never refer to the project, app, or tool as "PC Fixit", "pc-fixit", or "PC-Fixit".

**How to apply:**
- Always use `pcfix` for lowercase names, package names, keys, and identifiers.
- Always use `PCFix` or `PCFIX` for uppercase headers or titles.
- Never use "PC Fixit", "pc-fixit", or "pc fixit" anywhere in code, copy, titles, mockups, or communications.
